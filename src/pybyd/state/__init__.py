"""State/store layer.

This package is the single source of truth for how incoming data from
HTTP polling, MQTT events, and (future) push ingestion is merged into a
deterministic per-vehicle state snapshot.
"""
